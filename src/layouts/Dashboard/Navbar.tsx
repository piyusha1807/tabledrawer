import React from "react";
import { Box, AppBar, Toolbar } from "@mui/material";

const Navbar = () => {
  return (
    <AppBar>
      <Toolbar>
        <Box>
          <p>Hello Piyush</p>
        </Box>
      </Toolbar>
    </AppBar>
  );
};

export default Navbar;
